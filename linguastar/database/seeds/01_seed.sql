-- =============================================
-- LinguaStar Seeds
-- Insert test/initial data — run AFTER migration
-- =============================================

-- Add yourself as an admin (replace with your actual Supabase user ID)
-- Step 1: Sign up/login via the app to create your auth.users record
-- Step 2: Find your user ID in Supabase → Authentication → Users
-- Step 3: Run this with your user ID:

-- INSERT INTO public.admins (user_id)
-- VALUES ('YOUR-USER-UUID-HERE');

-- Example test book (optional)
-- INSERT INTO public.books (title, description, price, file_path)
-- VALUES (
--   'Sample Book',
--   'A sample language learning book',
--   299.00,
--   'books/sample.pdf'
-- );
